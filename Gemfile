source "https://rubygems.org"

# github-pages bundles jekyll-seo-tag and jekyll-sitemap already, so no need to
# list them separately here — they just need to be enabled in _config.yml plugins.
gem "github-pages", group: :jekyll_plugins
gem 'wdm', '>= 0.1.0' if Gem.win_platform?